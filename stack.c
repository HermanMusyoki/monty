#include "monty.h"
/**
 * f_stack - function that outputs the top of the stack
 * @head: double pointer to the beginning of the stack
 * @counter: parameter for the line count
 * Return: nothing
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
