#include "monty.h"
/**
 * stack_fn - function that outputs the top of the stack
 * @head: double pointer to the beginning of the stack
 * @counter: parameter for the line count
 * Return: nothing
*/
void stack_fn(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
